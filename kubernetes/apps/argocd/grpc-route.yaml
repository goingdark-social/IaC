apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: argocd-grpc
  namespace: argocd
spec:
  parentRefs:
    - name: external
      namespace: gateway
  hostnames:
    - "argocd.goingdark.social"
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80