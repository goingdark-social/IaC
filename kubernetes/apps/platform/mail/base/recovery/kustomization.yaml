apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Deliberately NOT referenced by base/kustomization.yaml - this is a base for the
# manual-only recovery overlay (see overlays/prod/recovery), not for the normal
# ArgoCD-synced deployment.

resources:
  - recovery-job.yaml
