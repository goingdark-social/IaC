apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: stalwart-web
  namespace: stalwart
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: stalwart
  policyTypes:
  - Ingress
  ingress:
  - ports:
    - port: 8080
      protocol: TCP