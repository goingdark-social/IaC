apiVersion: apps/v1
kind: Deployment
metadata:
  name: tor-proxy
  namespace: mastodon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tor-proxy
  template:
    metadata:
      labels:
        app: tor-proxy
    spec:
      containers:
        - name: proxy
          image: ghcr.io/avpnusr/torprivoxy:04-10-2025
          ports:
            - containerPort: 8118
              name: http
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 10m
              memory: 64Mi
