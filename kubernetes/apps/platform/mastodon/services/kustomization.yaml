apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mastodon

resources:
- web.yaml
- streaming.yaml
- sidekiq-generic.yaml
- sidekiq-scheduler.yaml

labels:
- pairs:
    app.kubernetes.io/part-of: mastodon
    tier: application

# Patches are applied at the overlay level (overlays/production)